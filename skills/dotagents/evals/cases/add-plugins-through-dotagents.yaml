behavior: add-plugins-through-dotagents
prompt: |
  Do not execute commands or modify files. Add the review-tools plugin from getsentry/agent-plugins to this repository and give me the exact command.
checks:
  - judge: The response uses one project-scoped dotagents add command with source `getsentry/agent-plugins` and name `review-tools`, and does not add a redundant install command because add installs immediately.
