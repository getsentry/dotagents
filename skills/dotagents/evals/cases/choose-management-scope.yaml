behavior: choose-management-scope
prompt: |
  Do not execute commands or modify files. This repository has agents.toml. Tell me the exact command you would use to install the find-bugs coding-agent skill from getsentry/skills.
checks:
  - judge: The response uses project-scoped dotagents add without `--user` because the repository has agents.toml.
